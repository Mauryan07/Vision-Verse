<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>Login</title>
    <style>
        body{
            font-size: 1rem;
            background: #f5f5f7;
        }
        .review{
            margin: 10px 5px;
            padding: 20px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 2px 4px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
        }
        .reviewer{
            font-weight: bold;
            font-size: 1.5rem;
            margin: 0;
            color: blueviolet;
        }
        .uid{
            font-size: 0.8rem;
            margin: 0;
            color: gray;
        }
        .reviewtext{
            font-size: 1.1rem;
            text-align: justify;
        }
        .bottom{
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            text-align: center;
            background: blueviolet;
            color: white;
        }
        a{
            color: white;
            font-weight: bold;
        }
    </style>
</head>

<body>

    

    


            <% if (product.ratings.length!==0){ %>

            <% product.ratings.forEach(function(rating){ %>



                <div class="review">
                    <p class="reviewer"> <%= rating.reviewer %></p>
                    <p class="uid"> <%= rating.postedby %></p>
                    <p class="reviewtext"> <%= rating.review %></p>
                    
                    


                    
                </div>
                <% }); %>

                <% } else { %>
                    <div >
                        <h2 style="font-weight: 700; font-size: 18px; text-align: center; ">No Reviews!</h2>
    
                    </div>
                    

                <% } %>





   
</body>

</html>